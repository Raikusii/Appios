workflows:
  android-workflow:
    name: Android Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    
    environment:
      flutter: stable
      xcode: latest
      node: latest
      groups:
        - google_credentials # Grupo para credenciales de Google
    scripts:
      - name: Get Flutter packages
        script: |
          cd flutter_application_taskapp
          flutter pub get
      - name: Flutter analyze
        script: |
          cd flutter_application_taskapp
          flutter analyze
      - name: Flutter test
        script: |
          cd flutter_application_taskapp
          flutter test
      - name: Build APK
        script: |
          cd flutter_application_taskapp
          flutter build apk --release
      - name: Build App Bundle
        script: |
          cd flutter_application_taskapp
          flutter build appbundle --release
    artifacts:
      - flutter_application_taskapp/build/app/outputs/flutter-apk/app-release.apk
      - flutter_application_taskapp/build/app/outputs/bundle/release/app-release.aab
    publishing:
      email:
        recipients:
          - tu@email.com # Reemplaza con tu email

  ios-workflow:
    name: iOS Build
    max_build_duration: 60
    instance_type: mac_mini_m2
    
    environment:
      flutter: stable
      xcode: latest
      node: latest
      groups:
        - apple_credentials # Grupo para credenciales de Apple
    scripts:
      - name: Get Flutter packages
        script: |
          cd flutter_application_taskapp
          flutter pub get
      - name: Flutter analyze
        script: |
          cd flutter_application_taskapp
          flutter analyze
      - name: Flutter test
        script: |
          cd flutter_application_taskapp
          flutter test
      - name: Build iOS
        script: |
          cd flutter_application_taskapp
          flutter build ios --release --no-codesign
      - name: Build IPA
        script: |
          cd flutter_application_taskapp
          flutter build ipa --release
    artifacts:
      - flutter_application_taskapp/build/ios/ipa/*.ipa
    publishing:
      email:
        recipients:
          - tu@email.com # Reemplaza con tu email
